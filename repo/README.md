# [farmnerdos] pacman repository

Generated output — run `../scripts/build-packages.sh` to populate `x86_64/`
with built packages and the `farmnerdos.db` database. Published to GitHub
Pages by `.github/workflows/build-repo.yml`. Contents are gitignored.
